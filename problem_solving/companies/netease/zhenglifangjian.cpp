#include <iostream>
#include <vector>
#include <utility>
#include <string>

using namespace std;

typedef pair<int, int> xypair;

bool is_square(xypair point[], int size){

    return false;
}

int main(int argc, char const *argv[]) {

    xypair point[4];
    xypair center[4];
    int n;

    while (std::cin >> n;) {
        cout<< n <<endl;
        while (n--) {
            for (size_t i = 0; i < 4; i++) {
                std::cin >> point[i].first >> point[i].second;
                std::cin >> center[i].first >> center[i].second;
            }

        }

    }

    return 0;
}






//
